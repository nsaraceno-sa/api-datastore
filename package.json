{
  "name": "mock-api-server",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "basic": "json-server --watch db.json --host 0.0.0.0 --port $PORT",
    "server": "json-server --watch db.json --routes routes.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
